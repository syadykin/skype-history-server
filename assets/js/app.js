var angular = require('angular'),
    bootstrap = require('angular-bootstrap'),
    jquery = require('jquery'),

    app = angular.module('app', [
        'ui.bootstrap'
    ]);

// app.controller('DemoCtrl', ['$scope', require('./controllers/demo')]);
